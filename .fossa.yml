version: 3

project:
  locator: solacecommunity_solace-terraform-provider
  id: solacecommunity_solace-terraform-provider
  name: solace-terraform-provider
  teams: []
  labels:
    - go

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
